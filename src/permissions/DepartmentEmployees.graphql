query addToDepartmentEmployees ($user_id: ID!) {
  SomeUserExists(filter: {
    id: $user_id,
    role: ADMIN
  })
}

query removeFromDepartmentEmployees ($user_id: ID!) {
  SomeUserExists(filter: {
    id: $user_id,
    role: ADMIN
  })
}
